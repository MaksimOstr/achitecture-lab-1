server:
  address: {{ app_bind_address }}
  port: {{ app_port }}

spring:
  datasource:
    url: jdbc:postgresql://{{ db_host }}:{{ db_port }}/{{ db_name }}
    username: {{ db_user }}
    password: {{ db_password }}
